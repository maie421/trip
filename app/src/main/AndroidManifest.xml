<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.trip">
    <uses-sdk android:targetSdkVersion="8" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />//앨범
    <uses-permission android:name="android.permission.INTERNET" /> ///php 넣기
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>//권한주기
    <uses-permission android:name="android.hardware.camera" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Trip"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".join"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        //android:theme="@style/AppTheme.NoActionBar"
        <activity
            android:name=".Plus"
            android:label="여 행 자"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".menu"
            android:label="여 행 자" />
        <activity
            android:name=".menu1"
            android:label="여 행 자"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Countrysetting"
            android:label="여 행 자"
            android:theme="@style/AppTheme" />
        <activity android:name=".Countrymenu" />
        <activity android:name=".Countryadd" />
        <activity android:name=".CountryDetail"></activity>
    </application>

</manifest>